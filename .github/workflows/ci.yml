name: Build

on:
  push:
    branches:
    - master
  pull_request:
env:
  GO111MODULE: on
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: golangci/golangci-lint-action@v2
  test:
    use mackerelio/workflows/.github/workflows/go-test.yml@main
